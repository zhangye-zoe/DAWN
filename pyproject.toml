[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dawn-nuclei"
version = "0.3.0"
description = "Refactored DAWN for domain-adaptive weakly supervised nuclei segmentation"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
